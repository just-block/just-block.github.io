{
  "name": "just-block",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npx concurrently -n landing,api,admin -c blue,green,magenta \"npm run dev -w landing\" \"npm run dev -w api\" \"npm run dev -w admin\"",
    "prettier:fix": "git ls-files -mo --exclude-standard -- './packages' | grep -E '\\.(ts|tsx|mts|json)$' | xargs -I {} prettier --write {}",
    "prettier:fix:full": "prettier --write \"./packages/**/*.+(ts|tsx|mts|json)\""
  }
}
